import styled from 'styled-components';
import { CssSizes } from '../../constants/styles.constants';

export const NoSavedEvents = styled.h1`
    margin: 1em auto;
    font-size: ${CssSizes['notifiction-desktop-font-size']};
    user-select: none;
    text-align: center;
`;
